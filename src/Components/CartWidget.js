import React from "react";
import * as Icon from 'react-bootstrap-icons';

const CartWidget = () => {
    return (
        <>
            <Icon.Cart color="royalblue" size={32} style={{marginRight: '50px'}}/>
        </>
    );
};
export default CartWidget;
